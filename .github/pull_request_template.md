## What changed

<!-- One or two sentences. What does this do that the previous state did not? -->

## Does the render change?

<!-- The point of this project is to be a reference model, so this is the
     question that matters most. Pick one: -->

- [ ] **No** — output is bit-identical (a refactor, a comment, docs, build files)
- [ ] **Yes, intentionally** — describe what changed visually and why it is correct
- [ ] **Unknown** — say so; that is a fine answer, but say it

## Verified by

<!-- What was actually run, not what should pass. For example:
     - make CXXFLAGS="-std=c++17 -Wall -Wextra -Werror -O2"
     - rendered 120 frames, compared frame 30 against the previous build
     - make docs with no new warnings -->

## Notes for later

<!-- Anything deferred, any assumption made, anything that will bite when the
     RTL implementation is diffed against this one. -->
